<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"        
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.naver.mapper.EmpathyMapper">
	
	<update id="updateEmpathyCount" parameterType="java.util.List">
		<foreach collection="list" item="empathy" index="index" separator=";">
        	UPDATE comment SET empathy = empathy + #{empathy.value}
        	WHERE id = #{empathy.key}
    	</foreach> 
	</update>
	
</mapper>